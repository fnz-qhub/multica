apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: multica
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: multica
data:
  PORT: "3000"
  HOSTNAME: "0.0.0.0"
  NODE_ENV: "production"
