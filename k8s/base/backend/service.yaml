apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: multica
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: multica
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
